// Placeholder kept so any stale `import LangToggle from '...'` doesn't crash
// the build. The app is English-only — language switching is removed.
// All callers were stripped; this file can be deleted once those imports
// are cleaned up.

import React from 'react';

export default function LangToggle(_props: { variant?: 'dark' | 'light' }) {
  return null;
}
